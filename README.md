# DHoloceneCliM-NA

**Downscaled Holocene Climate for North America**

A Python reimplementation and extension of the [DPastCliM-NA](https://github.com/Env-an-Stat-group/25.Guaita.DPastCliM-NA) statistical downscaling pipeline, producing monthly-resolution, station- and grid-level temperature and precipitation reconstructions spanning the last 22,000 years over North America.

## Method

The pipeline applies **Principal Component Regression (PCR)** to downscale coarse Earth System Model (ESM) transient simulations to the GHCN station network, with uncertainty quantification via:

1. **PCR calibration** on GHCN observations (1875-1999) with automatic PCA mode selection
2. **Spatial Error Model (SEM)** + **ARMA(1,1)** noise modeling for stochastic realizations
3. **Sibson natural-neighbor interpolation** to a 0.20 degree Albers Equal-Area grid
4. **Variance correction** (30-yr moving-window std matching) to align PCR variability with ESM

## Supported GCMs

| Model | Time span | Resolution | Reference |
|-------|-----------|------------|-----------|
| CCSM3 TraCE-21k II | 22 ka | 3.75 deg | He & Clark 2022 |
| MPI-ESM 1.2 CR | 26 ka | T31 (~3.75 deg) | Kapsch 2022 |

## Repository Structure

```
src/caz/                  Core Python package (PCR, SEM, gridding, IO)
Paper_Code/               Figure and table generation scripts
related_scripts/          Production pipeline drivers + MATLAB verification
tests/                    Smoke tests + MATLAB comparison
25.Guaita.DPastCliM-NA/   Reference MATLAB code (git submodule)
```

## Reproduction Guide

### Prerequisites

All scripts expect climate data at a configurable external path (default `D:\Dataset\DPastCliM-NA\`). Edit `Paper_Code/commons/_common.py` to set your local data directory.

Required external data:
- **GHCN observations**: monthly tas/pr station data (Parquet format, prepared by `related_scripts/prepare_ghcn.py`)
- **ESM outputs**: TraCE-21k II or MPI-ESM 1.2 CR monthly NetCDF files
- **Land mask**: `landmask_NA_020.nc` (generated by `related_scripts/make_static.py`)

### Pipeline Steps

Run all scripts with `python related_scripts/<script> --var {tas,pr}` unless noted otherwise.

#### Step 0: Data Preparation

| Script | Purpose |
|--------|---------|
| `prepare_ghcn.py` | Download and preprocess GHCN monthly data to Parquet |
| `make_static.py` | Generate 0.20 deg land mask for North America |
| `generate_split.py` | Create train/val/test split (cal 76 yr, val 38 yr, test 11 yr) |

#### Step 1: PCR Calibration

| Script | Purpose | Output |
|--------|---------|--------|
| `run_calibrate.py --var {tas,pr}` | Train monthly PCR models on cal-period GHCN + ESM | `pcr_models_{var}.pkl`, `pcr_diag_{var}.csv` |

#### Step 2: Noise Models (SEM + ARMA)

| Script | Purpose | Output |
|--------|---------|--------|
| `run_cal_pi.py --var {tas,pr}` | Fit per-month spatial (SEM) and temporal (ARMA) noise models | `sem_model_{var}_m{01-12}.pkl`, `arma_model_{var}_m{01-12}.pkl` |

#### Step 3: Station-Level Prediction

| Script | Purpose | Output |
|--------|---------|--------|
| `run_project.py --var {tas,pr} --cal-only` | Cal-period prediction (1875-1999) with PI | `recon_cal_{var}.csv` (det + PI + realization) |
| `run_project.py --var {tas,pr}` | Full 22 ka projection | `recon_station_{var}.csv` |

#### Step 4: Gridding

| Script | Purpose | Output |
|--------|---------|--------|
| `run_grid_cal.py --var {tas,pr}` | Grid cal/test fields to 0.20 deg (Sibson interpolation) | `grid_pcr_raw_{var}.nc`, `grid_obs_test_{var}.nc`, `grid_esm_cal_{var}.nc` |
| `run_grid_full_na.py --var {tas,pr}` | Grid full 22 ka realization | `gridded_{var}.nc` |

#### Step 5: Variance Correction

| Script | Purpose | Output |
|--------|---------|--------|
| `run_varcorr_cal.py --var {tas,pr}` | 30-yr moving-window std matching (cal period) | `grid_pcr_cal_{var}.nc` |

### Figure and Table Reproduction

All scripts in `Paper_Code/main/` read from the pipeline outputs above.

#### Main Paper

| Script | Figure/Table | Description |
|--------|-------------|-------------|
| `F1_station_coverage.py` | Fig. 1 | GHCN station spatial coverage |
| `F2_data_split.py` | Fig. 2 | Cal/val/test split + PCR mode selection |
| `F3_mean_fields.py` | Fig. 3 | Predict-window mean fields, PI width, anomalies (TraCE) |
| `F4_station_timeseries.py` | Fig. 4 | Station-level time series with PI bands |
| `F5_bias_map_tas.py` | Fig. 5 | Temperature mean bias + percentile maps (PCR vs Obs vs ESM) |
| `F6_bias_map_pr.py` | Fig. 6 | Precipitation mean bias + percentile maps |
| `T1_timestep_metrics.py` | Table 1 | Per-timestep validation metrics (RMSE, R2, bias) |
| `T2_station_metrics.py` | Table 2 | Per-station validation metrics |
| `T45_mss.py` | Table 4-5 | Model skill score summary |
| `TC1_vc_eval.py` | Table C1 | Variance correction evaluation |

#### Appendix (MPI-ESM)

| Script | Figure/Table | Description |
|--------|-------------|-------------|
| `FA1_station_coverage_mpi.py` | Fig. A1 | Station coverage for MPI-ESM |
| `FA2_data_split_mpi.py` | Fig. A2 | Data split for MPI-ESM |
| `FA3_mean_fields_mpi.py` | Fig. A3 | Predict-window fields (MPI-ESM) |
| `FA4_station_timeseries_mpi.py` | Fig. A4 | Station time series (MPI-ESM) |
| `FA5_bias_map_tas_mpi.py` | Fig. A5 | Temperature bias maps (MPI-ESM) |
| `FA6_bias_map_pr_mpi.py` | Fig. A6 | Precipitation bias maps (MPI-ESM) |

### MATLAB Verification

Paired Python/MATLAB scripts in `related_scripts/verification/` verify numerical consistency:

| Step | Verification |
|------|-------------|
| A | GHCN data loading |
| B | ESM loading and regridding |
| C | PCR calibration coefficients |
| D | Prediction + PI + delta-change |
| E | Gridding + variance correction |
| F | SEM/ARMA noise simulation |

## Installation

```bash
git clone --recurse-submodules https://github.com/px39n/26.Liang.DHoloceneCliM-NA.git
cd 26.Liang.DHoloceneCliM-NA

conda create -n caz python=3.11
conda activate caz
pip install -e ".[dev]"
```

## Related Work

This project builds on Guaita et al.'s MATLAB implementation ([25.Guaita.DPastCliM-NA](https://github.com/Env-an-Stat-group/25.Guaita.DPastCliM-NA)), extending it with multi-GCM support, Numba-accelerated gridding, and Python-native uncertainty quantification.

## License

See repository for license details.
